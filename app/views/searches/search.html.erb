<h1>検索結果</h1>
<table>
  <thead>
    <tr>
      <th>ホテル名</th>

    </tr>
  </thead>

  <tbody>
    <div><%= @results.count %></div>
    <% @results.each do |result| %>
      <tr>
        <td><%= result.name %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Top', root_path %>