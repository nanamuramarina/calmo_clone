<h1>お問合せ</h1>
<%= form_with model: @contact, url: contacts_path, method: :post  do |f| %>
  <h4>お名前</h4>
  <%= f.text_field :name %>

  <h4>メールアドレス</h4>
  <%= f.text_field :mail %>

  <h4>本文</h4>
  <%= f.text_area :message %>

  <%= f.submit '投稿' %>
<% end %>